Chp 1
methods 

fillBox
createPallet

vars
fullbox = 0
items = 60 , +1 fullbox
Pallet is equal to 0. then 10 fullbox is + one on pallet


items that equal some weights that constant

other tasks of getting our products sorted and ready to ship


fillbox method we'll say the the box fits up to 60 items if box is equal to or less then 60 then we'll fill the box till equal to 60 if this is true the box will go to the Pallet. 
if box is full then get new box. items are placed and count up to 60 then one fullbox us made.



createPallet.  We need to get data from fillbox for fullbox then a full pallet is 10 Fullbox so with some math like adding to its equal to 10. Then we need to had the pallet off somewhere 

If Fullbox is equal to 10 then we make one pallet 



Chp 2



Chp 3



Chp 4
def example(example_file):
    with open(example_file, mode='r') as csvfile:

fname = open("example.csv", 'r')

use the open method to use 

list store items
items = [0, 0, 0]

read lines
we need to spilt lines 
get numbers of each item listed in the example_file

def main
csv file name

csv file name



Chp 5
Change the items variable format from a list to a dictionary

Change the list format for items and columns to the dictionary format to align with the change above

Create a for loop for the output that prints a the output from a dictionary format for each item in the column